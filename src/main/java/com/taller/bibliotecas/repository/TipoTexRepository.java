package com.taller.bibliotecas.repository;

import com.taller.bibliotecas.entitys.TipoTex;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoTexRepository extends JpaRepository<TipoTex,Long> {
}
